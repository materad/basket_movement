version: '3.3'

services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    # environment: 
    #   MONGO_INITDB_DATABASE: GameMovementLog
  
  mongodb-seed:
    build: ./Services/Movement
    depends_on:
      - mongodb
